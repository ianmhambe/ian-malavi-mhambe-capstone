apiVersion: v1
kind: Secret
metadata:
  name: caresync-secrets
  namespace: ian-malavi-mhambe
  labels:
    app.kubernetes.io/name: caresync
type: Opaque
stringData:
  # IMPORTANT: Replace these with actual secrets in production
  # Use AWS Secrets Manager or HashiCorp Vault in production
  DATABASE_URL: "postgresql://caresync:CHANGE_ME@caresync-postgres:5432/caresync_db?schema=public"
  JWT_SECRET: "CHANGE_ME_TO_A_SECURE_SECRET_AT_LEAST_32_CHARACTERS"
  JWT_REFRESH_SECRET: "CHANGE_ME_TO_ANOTHER_SECURE_SECRET_AT_LEAST_32_CHARACTERS"
  POSTGRES_USER: "caresync"
  POSTGRES_PASSWORD: "CHANGE_ME"
  POSTGRES_DB: "caresync_db"
